     // 初始化Lucide图标
        lucide.createIcons();

        // 下载处理函数（跳转逻辑保持不变）
        function handleDownload(title) {
            if (confirm(`即将跳转 ${title}\n请确认网络环境无异常`)) {
                window.location.href = "b.html"; // 跳转到指定页面
            }
        }
function cba(title) {
            if (confirm(`即将跳转 ${title}\n请确认网络环境无异常`)) {
                window.location.href = "c.html"; // 跳转到指定页面
            }
        }
function gta(title) {
            if (confirm(`即将跳转 ${title}\n请确认网络环境无异常`)) {
                window.location.href = "d.html"; // 跳转到指定页面
            }
        }
function gbj(title) {
            if (confirm(`即将跳转 ${title}\n请确认网络环境无异常`)) {
                window.location.href = "e.html"; // 跳转到指定页面
            }
        }
       function cfa(title) {
            if (confirm(`即将跳转 ${title}\n请确认网络环境无异常`)) {
                window.location.href = "f.html"; // 跳转到指定页面
            }
        }
        function kfa(title) {
            if (confirm(`即将跳转 ${title}\n请确认网络环境无异常`)) {
                window.location.href = "g.html"; // 跳转到指定页面
            }
        }